# 本公司81_数电专票→长软09(QDLX=20) 字段映射表

**文档版本**: v1.1
**创建日期**: 2025-12-30
**最后更新**: 2025-12-30
**发票类型**: 81 - 电子发票(增值税专用发票) / 数字化电子发票-增值税专用发票

---

## 📋 概述

本文档定义了本公司发票查验API与长软发票查验服务之间，针对"数电专票"的完整字段映射关系。

- **本公司API**: [`/partners/invoice-verifications`](https://s.apifox.cn/2795ad32-31e7-4363-8294-578e8dc6202c/395314528e0) (RESTful JSON格式)
- **长软API**: 【长软】发票查验服务调用说明V4.0.16.pdf (XML格式)
- **本公司发票类型代码**: 81
- **长软发票类型代码**: 09 (数电发票)
- **长软数电发票类型**: QDLX=20 (专票)
- **长软文档章节**: 3.2.3.9

### 依赖的通用文档

本映射表引用以下通用文档，使用前请先了解：
- 📄 [00_通用说明_请求参数映射.md](./00_通用说明_请求参数映射.md) - 查验请求参数映射
- 📄 [00_通用说明_枚举映射.md](./00_通用说明_枚举映射.md) - 通用枚举值映射（CYJGDM, ZFBZ, TSPZBZ等）

---

## 一、类型映射关系

| 项目 | 本公司API | 长软API | 说明 |
|------|-----------|---------|------|
| 发票类型代码 | `81` | `09` | 长软数电发票统一类型代码 |
| 数电发票子类型 | - | `QDLX=20` | **关键**：长软用QDLX区分数电子类型 |
| 发票类型名称 | 电子发票(增值税专用发票) | 数电发票(专票) | |
| 数据结构定义 | `#/definitions/231697052` | `3.2.3.9` | 本定义也包含82/85/86类型 |
| 发票介质 | 全电子 | 全电子 | 无纸质发票代码 |

---

## 二、发票主信息字段映射

### 2.1 完整字段对照表

⚠️ **重要**: 本表格包含该发票类型的**所有响应字段**，请逐一核对。

| 本公司API字段路径 | 本公司API中文名 | 本公司API类型 | 长软API字段路径 | 长软API中文名 | 长软API类型 | 映射说明 |
|---|---|---|---|---|---|---|
| `invoice_number` | 发票号码 | string(20) | `BODY/FPHM` | 发票号码 | VARCHAR2(20) | 直接映射，数电票号20位 |
| `invoice_code` | 纸质发票代码 | string(12) | `BODY/FPDM` | 发票代码 | VARCHAR2(20) | **数电专票无发票代码**，FPDM为空 |
| `paper_invoice_no` | 纸质发票号码 | string(20) | - | - | - | **本公司独有**，数电票无此字段 |
| - | - | - | `BODY/CYCS` | 查验次数 | VARCHAR2(32) | **长软独有**，记录查验次数 |
| `issue_date` | 开票日期 | string(19) | `BODY/KPRQ` | 开票日期 | VARCHAR2(32) | 格式转换：YYYY-MM-DD ← YYYYMMDD |
| `buyer_name` | 购买方名称 | string(150) | `BODY/GFMC` | 购方名称 | VARCHAR2(300) | 直接映射 |
| `buyer_tax_no` | 购买方识别号 | string(20) | `BODY/GFSBH` | 购方纳税人识别号 | VARCHAR2(64) | 直接映射 |
| `buyer_address` | 购买方地址 | string(300) | `BODY/GFDZDH` | 购方地址、电话 | VARCHAR2(120) | **长软字段为空** |
| `buyer_phone` | 购买方电话 | string(60) | `BODY/GFDZDH` | 购方地址、电话 | VARCHAR2(120) | **长软字段为空** |
| `buyer_bank_name` | 购买方开户行 | string(120) | `BODY/GFYHZH` | 购方开户行及账号 | VARCHAR2(120) | **长软字段为空** |
| `buyer_account_number` | 购买方账号 | string(100) | `BODY/GFYHZH` | 购方开户行及账号 | VARCHAR2(120) | **长软字段为空** |
| `seller_name` | 销售方名称 | string(150) | `BODY/XFMC` | 销方名称 | VARCHAR2(300) | 直接映射 |
| `seller_tax_no` | 销售方识别号 | string(20) | `BODY/XFSBH` | 销方纳税人识别号 | VARCHAR2(26) | 直接映射 |
| `seller_address` | 销货方地址 | string(250) | `BODY/XFDZDH` | 销方地址电话 | VARCHAR2(120) | **长软字段为空** |
| `seller_phone` | 销售方电话 | string(60) | `BODY/XFDZDH` | 销方地址电话 | VARCHAR2(120) | **长软字段为空** |
| `seller_bank_name` | 销售方开户行 | string(120) | `BODY/XFYHZH` | 销方开户行及账号 | VARCHAR2(120) | **长软字段为空** |
| `seller_account_number` | 销售方账号 | string(100) | `BODY/XFYHZH` | 销方开户行及账号 | VARCHAR2(120) | **长软字段为空** |
| - | - | - | `BODY/JE` | 金额合计 | VARCHAR2(32) | **长软独有**，本公司从items计算 |
| `total_tax_amount` | 合计税额 | number(18,2) | `BODY/SE` | 税额合计 | VARCHAR2(32) | 类型转换：长软为字符串 |
| `total_amount` | 价税合计 | number(18,2) | `BODY/JSHJ` | 价税合计 | VARCHAR2(32) | 类型转换：长软为字符串 |
| `amount_with_tax_in_words` | 价税合计(大写) | string(300) | - | - | - | **本公司独有**，需自行计算 |
| `remark` | 备注 | string(450) | `BODY/BZ` | 备注 | VARCHAR2(610) | 直接映射 |
| - | - | - | `BODY/SDSJBZ` | 数电发票实际备注 | VARCHAR2(750) | **长软独有**，数电票特有备注 |
| - | - | - | `BODY/JQBH` | 机器编号 | VARCHAR2(16) | **长软字段为空**，数电票无税控设备 |
| - | - | - | `BODY/JYM` | 校验码 | VARCHAR2(32) | **长软字段为空**，数电票无校验码 |
| `invoice_status` | 发票状态 | 枚举 | `BODY/ZFBZ` | 作废标志 | VARCHAR2(2) | 枚举映射：参见通用枚举文档 |
| - | - | - | `BODY/CPYBZ` | 成品油标志 | VARCHAR2(2) | 废止，以TSPZBZ为准 |
| - | - | - | `BODY/CYSJ` | 查验时间 | VARCHAR2(20) | **长软独有**，查验时间戳 |
| - | - | - | `BODY/QDBZ` | 清单标志 | VARCHAR2(2) | **长软独有**，参见通用枚举 |
| - | - | - | `BODY/TSPZBZ` | 特殊票种标志 | VARCHAR2(2) | 枚举映射：参见通用枚举文档 |
| - | - | - | `BODY/HZDK` | 汇总代开标志 | VARCHAR2(2) | **长软独有**，参见通用枚举 |
| `issuer` | 开票人 | string(300) | - | - | - | **本公司独有**，数电票无此字段 |
| `reviewer` | 复核人姓名 | string(75) | `BODY/SDFHR` | 数电发票复核人 | VARCHAR2(150) | 直接映射，可空 |
| `payee` | 收款人姓名 | string(150) | `BODY/SDSKR` | 数电发票收款人 | VARCHAR2(150) | 直接映射，可空 |
| `is_blue_invoice` | 蓝字/红字标识 | 枚举(Y/N) | - | - | - | **本公司独有**，从ZFBZ推断 |
| `original_blue_invoice_no` | 对应蓝字发票号码 | string(20) | - | - | - | **本公司独有**，红票时填写 |
| `seller_taxpayer_type_code` | 销方纳税人类型代码 | string | - | - | - | **本公司独有**，1=一般纳税人 |
| `item_count` | 明细条数 | integer | - | - | - | **本公司独有**，items数组长度 |
| `deduction_amount` | 扣除额 | number(18,2) | - | - | - | **本公司独有**，差额征税扣除额 |
| `settlement_method_code` | 结算方式 | string(2) | - | - | - | **本公司独有** |
| `vat_immediate_refund_code` | 增值税即征即退代码 | string(2) | - | - | - | **本公司独有** |
| `export_tax_refund_type_code` | 出口退税类代码 | string(2) | - | - | - | **本公司独有** |
| `refined_oil_abnormal_flag` | 成品油异常标识 | string(1) | - | - | - | **本公司独有**，9=正常 |
| - | - | - | `HEAD/CYJGDM` | 查验结果代码 | VARCHAR(3) | **关键字段**，查验状态码，参见通用枚举文档 |

**字段分类说明**：
- **直接映射**: 字段名和含义一致，直接对应
- **格式转换**: 需要进行格式转换（如日期、金额格式）
- **类型转换**: 需要进行数据类型转换（如string ↔ number）
- **长软独有**: 长软API独有字段，本公司API无此字段
- **本公司独有**: 本公司API独有字段，需从其他数据源获取或计算
- **长软字段为空**: 数电票特性导致长软也返回空值

### 2.2 本公司独有字段处理说明

| 本公司字段 | 数据来源 | 处理逻辑 |
|-----------|---------|----------|
| `amount_with_tax_in_words` | 计算得出 | 将total_amount数字转换为中文大写 |
| `item_count` | 计算得出 | items数组长度，即明细条数 |
| `is_blue_invoice` | 推断得出 | 根据ZFBZ判断：0/1→Y(蓝字)，3/7/8→N(红字) |
| `original_blue_invoice_no` | 业务系统 | 红字发票对应的原蓝字发票号，从业务系统获取 |
| `seller_taxpayer_type_code` | 业务规则 | 数电专票一般都是一般纳税人开具，固定为"1" |
| `deduction_amount` | 计算得出 | 差额征税时的扣除额，从items中计算 |
| `issuer` | - | 数电票无开票人字段，返回null |
| `paper_invoice_no` | - | 数电票无纸质号码，返回null |
| `refined_oil_abnormal_flag` | 业务规则 | 成品油异常标识，非成品油票固定为"9" |

---

## 三、货物/明细字段映射

### 3.1 明细结构说明

**本公司API路径**: `items[]`
**长软API路径**: `BODY/CHILDLIST/CHILD[]`

⚠️ **注意**:
- ✅ **数电专票有货物明细结构**
- 明细数据记录货物或应税劳务的详细信息
- 一张发票可以有多个明细行
- 数电票明细字段与传统专票完全一致

### 3.2 完整明细字段对照表

| 本公司API字段 | 本公司API中文名 | 本公司API类型 | 长软API字段 | 长软API中文名 | 长软API类型 | 映射说明 |
|---|---|---|---|---|---|---|
| `sequence_no` | 序号 | integer | - | - | - | **本公司独有**，用于排序 |
| `name` / `goods_or_service_name` | 货物或应税劳务名称 | string(300/600) | `HWMC` | 货物或应税劳务名称 | VARCHAR2(320) | 直接映射 |
| `specification` | 规格型号 | string(150) | `GGXH` | 规格型号 | VARCHAR2(64) | 直接映射 |
| `unit` | 单位 | string(300) | `DW` | 单位 | VARCHAR2(52) | 直接映射 |
| `quantity` | 发票交易数量 | string(25) | `SL` | 数量 | VARCHAR2(25) | 直接映射 |
| `unit_price` | 发票交易单价 | string(25) | `DJ` | 单价 | VARCHAR2(25) | 直接映射 |
| `amount` | 金额 | number(8,2) | `JE` | 金额 | NUMBER(20,2) | 直接映射 |
| `tax_rate` | 税率 | number(6,6) | `SLV` | 税率 | NUMBER(8,5) | 直接映射 |
| `tax_amount` | 税额 | number(8,6) | `SE` | 税额 | NUMBER(20,2) | 直接映射 |
| `tax_classification_code` | 商品税收分类编码 | string(19) | - | - | - | **本公司独有** |
| `deduction_amount` | 扣除额 | number(8,2) | - | - | - | **本公司独有**，差额征税 |
| `item_short_name` | 商品服务简称 | string(120) | - | - | - | **本公司独有** |
| `product_barcode` | 商品条码 | string(300) | - | - | - | **本公司独有** |

**说明**：
- 长软API中明细字段的金额、税额、税率都是NUMBER类型，可以直接映射
- 其他字段（数量、单价）都是字符串类型
- 本公司独有字段需要从业务系统或Apifox定义中获取
- 数电专票的明细结构与传统增值税专用发票（01）完全一致

### 3.3 特定要素字段说明 ⚠️ 重要

**背景说明**：

本公司API定义（`definitions/231697052`）中包含了**11种特定要素字段**，用于记录特殊行业或特殊业务场景下的扩展信息。这些字段在以下发票类型中共享：
- 81 - 电子发票(增值税专用发票)
- 82 - 电子发票(普通发票)
- 85 - 纸质发票(增值税专用发票)
- 86 - 纸质发票(普通发票)

**关键结论**：⚠️ **长软API完全不返回这些特定要素字段！**

根据对长软文档（`【长软】发票查验服务调用说明V4.0.16.pdf` 第3.2.3.9节 和 第3.2.3.10节）的详细核查，长软返回的数电发票响应结构中**不包含**任何特定要素字段。

#### 3.3.1 特定要素字段清单

以下是本公司API定义中的所有特定要素字段：

| 序号 | 本公司API字段名 | 中文名称 | 应用场景 | 长软API对应字段 |
|------|----------------|---------|---------|---------------|
| 1 | `construction_service_detail_list` | 建筑服务特定要素列表 | 建筑服务发票 | ❌ 无 |
| 2 | `freight_transport_service_detail_list` | 货物运输服务特定要素列表 | 货物运输发票 | ❌ 无 |
| 3 | `real_estate_sales_detail_list` | 不动产销售服务特定要素列表 | 不动产销售发票 | ❌ 无 |
| 4 | `real_estate_lease_detail_list` | 不动产租赁服务特定要素列表 | 不动产租赁发票 | ❌ 无 |
| 5 | `vehicle_vessel_tax_collection` | 车船税征收特定要素 | 代收车船税发票 | ❌ 无 |
| 6 | `toll_fee_detail_list` | 通行费特定要素列表 | 通行费发票 | ❌ 无 |
| 7 | `passenger_transport_service_detail_list` | 旅客运输服务特定要素列表 | 旅客运输发票 | ❌ 无 |
| 8 | `medical_service_inpatient_elements` | 医疗服务-住院特定要素 | 医疗服务发票(住院) | ❌ 无 |
| 9 | `medical_service_outpatient_elements` | 医疗服务-门诊特定要素 | 医疗服务发票(门诊) | ❌ 无 |
| 10 | `tractor_harvester_elements` | 拖拉机收割机特定要素 | 拖拉机/收割机发票 | ❌ 无 |
| 11 | `used_vehicle_elements` | 二手车特定要素 | 二手车销售发票 | ❌ 无 |

#### 3.3.2 影响与处理策略

**对API集成的影响**：

1. **查验接口无法返回特定要素**
   - 长软查验服务只返回发票基本信息和货物明细
   - 无法通过查验接口获取任何特定要素数据
   - 这11个字段在查验响应中将**始终为空**（`null` 或 `[]`）

2. **需要其他数据源补充**
   - 如果业务需要这些特定要素数据，需要从其他渠道获取：
     - 📄 **发票原始文件**：解析PDF/OFD电子发票文件
     - 🏢 **业务系统**：从企业ERP、财务系统等获取
     - 🔌 **其他API**：使用OCR识别或其他第三方服务
     - 💾 **本地缓存**：如果之前已采集过，从数据库读取

3. **推荐处理策略**

**方案A：仅返回基本信息**（推荐）
```json
{
  "result_code": "00",
  "invoice_number": "12345678901234567890",
  "total_amount": 11300.00,

  // 特定要素字段返回null或空数组
  "construction_service_detail_list": null,
  "freight_transport_service_detail_list": null,
  // ... 其他特定要素字段同样为null
}
```

**方案B：从其他数据源补充**
```javascript
// 伪代码：集成多数据源
async function enrichInvoiceData(changRuanResult, invoiceNumber) {
  // 1. 从长软获取基本信息
  const baseInfo = mapChangRuanToCompanyAPI(changRuanResult);

  // 2. 尝试从业务系统获取特定要素（可选）
  const specialElements = await getSpecialElementsFromBusinessSystem(invoiceNumber);

  // 3. 合并数据
  return {
    ...baseInfo,
    construction_service_detail_list: specialElements?.construction || null,
    freight_transport_service_detail_list: specialElements?.freight || null,
    // ... 其他特定要素
  };
}
```

**方案C：文档化限制**
- 在API文档中明确说明：查验接口不返回特定要素字段
- 提供单独的接口用于获取特定要素（如果有其他数据源）
- 在响应schema中将这些字段标记为"仅在特定情况下返回"

#### 3.3.3 业务理解

**为什么长软不返回特定要素？**

1. **定位差异**
   - 长软查验服务的核心功能是**验证发票真伪**
   - 特定要素属于**业务详情**，不是查验必需信息
   - 税局查验接口也不包含特定要素数据

2. **数据来源**
   - 特定要素通常来源于开票系统的业务录入
   - 查验服务只能访问税局的发票基础数据
   - 完整的特定要素信息需要从发票原始文件获取

3. **适用场景**
   - 这些特定要素主要用于：
     - 企业内部财务核算
     - 税务申报明细
     - 成本费用归集
     - 合同结算对账
   - 并非发票查验的核心需求

#### 3.3.4 集成建议

✅ **推荐做法**：

1. **查验阶段**：只返回长软提供的基本信息，特定要素字段返回 `null`
2. **详情展示**：如需特定要素，提供单独的"发票详情"接口，从其他数据源获取
3. **文档说明**：在API文档中明确标注这些字段的数据来源和可用性

❌ **不推荐做法**：

1. ~~期望长软返回特定要素~~ - 长软不支持
2. ~~因为缺少特定要素而拒绝返回查验结果~~ - 会影响基本查验功能
3. ~~尝试从发票号码推测特定要素~~ - 不可靠

---

## 四、数据格式转换说明

### 4.1 日期格式转换

| API | 格式 | 示例 |
|-----|------|------|
| 本公司API | `YYYY-MM-DD` 或 `YYYY-MM-DD HH:mm:ss` | 2025-12-30 或 2025-12-30 16:59:45 |
| 长软API | `YYYYMMDD` | 20251230 |

**转换规则**：
```javascript
// 长软 → 本公司
function formatDate(yyyymmdd) {
  if (!yyyymmdd || yyyymmdd.length !== 8) return null;
  return `${yyyymmdd.substr(0,4)}-${yyyymmdd.substr(4,2)}-${yyyymmdd.substr(6,2)}`;
}
// 示例: "20251230" → "2025-12-30"
```

### 4.2 数值类型转换

| 数据项 | 本公司API | 长软API | 转换说明 |
|--------|-----------|---------|----------|
| 金额合计 | 本公司从items计算 | `VARCHAR2(32)` 字符串 | 长软JE字段存在但本公司不使用 |
| 税额合计 | `number` (18,2) | `VARCHAR2(32)` 字符串 | 长软→本公司: `parseFloat()` |
| 价税合计 | `number` (18,2) | `VARCHAR2(32)` 字符串 | 长软→本公司: `parseFloat()` |
| 明细金额 | `number` (8,2) | `NUMBER(20,2)` | 类型一致，直接映射 |
| 明细税额 | `number` (8,6) | `NUMBER(20,2)` | 精度不同，注意转换 |
| 明细税率 | `number` (6,6) | `NUMBER(8,5)` | 精度不同，注意转换 |

**转换示例**：
```javascript
// 长软API返回
{
  "JE": "10000.00",      // 金额合计（字符串）
  "SE": "1300.00",       // 税额合计（字符串）
  "JSHJ": "11300.00"     // 价税合计（字符串）
}

// 转换为本公司API
{
  "total_tax_amount": 1300.00,
  "total_amount": 11300.00,
  "amount_with_tax_in_words": "壹万壹仟叁佰元整"
}
```

### 4.3 字符串长度适配

| 字段 | 本公司长度 | 长软长度 | 处理策略 |
|------|-----------|---------|----------|
| invoice_number | 20 | 20 | ✅ 长度一致，数电票号20位 |
| seller_tax_no | 20 | 26 | ⚠️ 长软更宽松，但实际18位够用 |
| buyer_tax_no | 20 | 64 | ⚠️ 长软更宽松，但实际18位够用 |
| buyer_name | 150 | 300 | ⚠️ 长软更大，需注意截断 |
| seller_name | 150 | 300 | ⚠️ 长软更大，需注意截断 |

---

## 五、枚举值映射

### 5.1 通用枚举映射

通用枚举值详见: 📄 [00_通用说明_枚举映射.md](./00_通用说明_枚举映射.md)

包括：
- **查验结果代码** (CYJGDM → result_code)
- **作废标志** (ZFBZ → invoice_status)
- **特殊票种标志** (TSPZBZ → special_invoice_type)
- **清单标志** (QDBZ)
- **汇总代开标志** (HZDK)

### 5.2 数电专票特有枚举映射

⚠️ **重要**: 数电专票**没有特有枚举**，所有枚举都使用通用枚举映射。

但需要注意以下特殊情况：

#### 5.2.1 发票类型区分 (QDLX)

数电发票统一使用长软类型代码09，通过QDLX字段区分子类型：

| QDLX值 | 含义 | 本公司发票类型 |
|-------|------|--------------|
| 20 | 数电专票（电子） | 81 |
| 10 | 数电普票（电子） | 82 |
| 01 | 数电纸质专票 | 85 |
| 04 | 数电纸质普票 | 86 |
| 03 | 数电机动车（电子） | 83 |
| 15 | 数电二手车（电子） | 84 |

**说明**：
- 长软在消息头 `HEAD/QDLX` 返回此字段
- **本映射表仅处理QDLX=20（数电专票）**
- 其他QDLX值应路由到对应的映射表

#### 5.2.2 发票状态的特殊性

数电发票的发票状态（ZFBZ）与传统发票一致，但需要注意：

| ZFBZ值 | 含义 | 说明 |
|-------|------|------|
| 0 或 1 | 正常 | 数电票正常状态 |
| 2 | 作废 | 数电票作废（很少见） |
| 3 | 红冲 | 红字发票（数电票常用） |
| 7 | 部分红冲 | 部分红字 |
| 8 | 全额红冲 | 全额红字 |

**数电票的特殊性**：
- 数电票很少使用"作废"（ZFBZ=2），通常使用"红冲"（ZFBZ=3）
- 红字数电票会在 `original_blue_invoice_no` 中记录被红冲的蓝字发票号

---

## 六、数电专票特殊性说明

### 6.1 该类型的独特特性

1. **无发票代码**
   - 数电专票**没有发票代码**字段（FPDM为空）
   - 仅有20位发票号码作为唯一标识
   - 这是与传统专票（01）的最大区别

2. **无校验码**
   - 数电专票**没有校验码**字段（JYM为空）
   - 防伪验证依赖电子签名等技术

3. **无税控设备信息**
   - **没有机器编号**（JQBH为空）
   - 数电票通过电子税务局开具，无需税控设备

4. **四方信息简化**
   - 长软返回的 `GFDZDH`、`GFYHZH`、`XFDZDH`、`XFYHZH` 均为**空**
   - 数电票不强制要求地址电话、开户行账号信息
   - 仅保留购销双方名称和税号

5. **可抵扣进项税**
   - 数电专票与传统专票效力相同，可以抵扣进项税额
   - 一般纳税人取得数电专票可按票面税额抵扣

6. **必须有货物明细**
   - 数电专票必须包含货物或应税劳务明细
   - CHILDLIST不能为空
   - 每个明细行需要包含完整的税率、税额信息

7. **红字发票处理**
   - 数电票很少作废，通常通过开具红字发票冲销
   - 红字发票需要记录对应的原蓝字发票号码

8. **数电特有字段**
   - `SDSKR`（数电发票收款人）：可空
   - `SDFHR`（数电发票复核人）：可空
   - `SDSJBZ`（数电发票实际备注）：可空，可能与BZ不同

### 6.2 与其他类型的区别

| 特性 | 数电专票(81) | 增值税专用发票(01) | 电子专票(08) | 数电普票(82) |
|------|-------------|------------------|-------------|-------------|
| 发票代码 | ❌ 无 | ✅ 12位 | ✅ 12位 | ❌ 无 |
| 发票号码 | ✅ 20位 | ✅ 8位 | ✅ 8位 | ✅ 20位 |
| 校验码 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 |
| 抵扣 | ✅ 可抵扣 | ✅ 可抵扣 | ✅ 可抵扣 | ❌ 不可抵扣 |
| 明细结构 | ✅ 有货物明细 | ✅ 有货物明细 | ✅ 有货物明细 | ✅ 有货物明细 |
| 四方信息 | ⚠️ 简化 | ✅ 完整 | ✅ 完整 | ⚠️ 简化 |
| 发票介质 | 电子 | 纸质 | 电子 | 电子 |
| 长软类型码 | 09 (QDLX=20) | 01 | 20 | 09 (QDLX=10) |
| 税控设备 | ❌ 无 | ✅ 有 | ✅ 有 | ❌ 无 |

### 6.3 特殊处理逻辑

#### 6.3.1 发票类型识别

由于长软对所有数电票统一返回类型码09，必须通过QDLX区分：

```javascript
// 伪代码：数电发票类型识别
function identifyDigitalInvoiceType(head) {
  const fplx = head.FPLX;  // "09"
  const qdlx = head.QDLX;  // 数电子类型

  if (fplx !== '09') {
    return null;  // 非数电发票
  }

  const mapping = {
    '20': '81',  // 数电专票
    '10': '82',  // 数电普票
    '01': '85',  // 数电纸质专票
    '04': '86',  // 数电纸质普票
    '03': '83',  // 数电机动车
    '15': '84'   // 数电二手车
  };

  return mapping[qdlx] || null;
}
```

#### 6.3.2 四方信息处理

数电票的四方信息简化，需要特殊处理：

```javascript
// 伪代码：数电票四方信息处理
function processDigitalInvoiceParties(body) {
  return {
    // 购方信息
    buyer_name: body.GFMC,
    buyer_tax_no: body.GFSBH,
    buyer_address: null,        // 数电票无此字段
    buyer_phone: null,          // 数电票无此字段
    buyer_bank_name: null,      // 数电票无此字段
    buyer_account_number: null, // 数电票无此字段

    // 销方信息
    seller_name: body.XFMC,
    seller_tax_no: body.XFSBH,
    seller_address: null,        // 数电票无此字段
    seller_phone: null,          // 数电票无此字段
    seller_bank_name: null,      // 数电票无此字段
    seller_account_number: null, // 数电票无此字段
    seller_taxpayer_type_code: '1'  // 数电专票一般都是一般纳税人
  };
}
```

#### 6.3.3 金额计算验证

数电票的金额计算与传统发票一致：

```javascript
// 伪代码：验证数电专票金额逻辑
function validateDigitalInvoiceAmount(body) {
  const totalAmount = parseFloat(body.JSHJ);     // 价税合计
  const totalTax = parseFloat(body.SE);          // 税额合计
  const totalGross = parseFloat(body.JE);        // 金额合计（长软返回）

  // 验证：价税合计 = 金额合计 + 税额合计
  const calculated = totalGross + totalTax;
  const diff = Math.abs(calculated - totalAmount);

  if (diff > 0.02) {  // 允许2分钱误差
    console.warn(`数电票金额计算不一致: ${calculated} != ${totalAmount}`);
  }

  // 明细金额验证
  const items = body.CHILDLIST.CHILD;
  const itemsGross = items.reduce((sum, item) => sum + parseFloat(item.JE || 0), 0);
  const itemsTax = items.reduce((sum, item) => sum + parseFloat(item.SE || 0), 0);

  return {
    total_amount: totalAmount,
    total_tax_amount: totalTax,
    is_valid: diff <= 0.02
  };
}
```

#### 6.3.4 红字发票处理

数电票的红字处理逻辑：

```javascript
// 伪代码：数电红字发票处理
function processRedInvoice(body, zfbz) {
  const isRedInvoice = ['3', '7', '8'].includes(zfbz);

  return {
    is_blue_invoice: isRedInvoice ? 'N' : 'Y',
    invoice_status: parseInt(zfbz),

    // 红字发票需要记录原蓝字发票号
    // 注意：长软09类型暂不返回原票号，需从业务系统获取
    original_blue_invoice_no: isRedInvoice ?
      getOriginalBlueInvoiceNo(body.FPHM) : null
  };
}
```

---

## 七、集成注意事项

### 7.1 必填字段对比

**长软API要求必填**（但本公司API可选）:
- 无特殊要求

**本公司API要求必填**（但长软API可选）:
- `invoice_number` (发票号码) - 必填，20位数字
- `seller_name` (销售方名称) - 必填
- `buyer_name` (购买方名称) - 必填
- `total_amount` (价税合计) - 必填
- `total_tax_amount` (合计税额) - 必填
- `amount_with_tax_in_words` (价税合计大写) - 必填，需自行计算
- `is_blue_invoice` (蓝字/红字标识) - 必填，从ZFBZ推断
- `invoice_status` (发票状态) - 必填，从ZFBZ映射

### 7.2 字段长度差异

| 字段 | 本公司长度 | 长软长度 | 实际风险评估 |
|------|-----------|---------|------------|
| invoice_number | 20 | 20 | ✅ 长度一致，数电票号20位 |
| seller_tax_no | 20 | 26 | ⚠️ 长软更大，但中国税号18位，20位足够 |
| buyer_tax_no | 20 | 64 | ⚠️ 长软更大，但中国税号18位，20位足够 |
| buyer_name | 150 | 300 | ⚠️ 长软更大，需截断超长名称 |
| seller_name | 150 | 300 | ⚠️ 长软更大，需截断超长名称 |

### 7.3 数据完整性

**长软独有字段**（本公司API无对应）:
- `CYCS` (查验次数) - 长软统计查验次数
- `CYSJ` (查验时间) - 长软查验时间戳
- `JE` (金额合计) - 长软返回，本公司从items计算
- `FPDM` (发票代码) - 数电票为空
- `JQBH` (机器编号) - 数电票为空
- `JYM` (校验码) - 数电票为空
- `GFDZDH` (购方地址电话) - 数电票为空
- `GFYHZH` (购方开户行账号) - 数电票为空
- `XFDZDH` (销方地址电话) - 数电票为空
- `XFYHZH` (销方开户行账号) - 数电票为空
- `QDBZ` (清单标志) - 是否有销货清单
- `HZDK` (汇总代开标志) - 是否汇总代开
- `SDSJBZ` (数电发票实际备注) - 数电票特有备注

**本公司独有字段**（需从其他数据源获取）:
- `amount_with_tax_in_words` (价税合计大写) - 从total_amount计算
- `item_count` (明细条数) - 从items数组长度计算
- `is_blue_invoice` (蓝字/红字标识) - 从ZFBZ推断
- `original_blue_invoice_no` (原蓝字发票号) - 从业务系统获取
- `paper_invoice_no` (纸质发票号码) - 数电票无此字段
- `invoice_code` (发票代码) - 数电票无此字段
- `issuer` (开票人) - 数电票无此字段
- `seller_taxpayer_type_code` (销方纳税人类型) - 固定为"1"（一般纳税人）
- `deduction_amount` (扣除额) - 从items计算
- `settlement_method_code` (结算方式) - 从业务系统获取
- `vat_immediate_refund_code` (即征即退代码) - 从业务系统获取
- `export_tax_refund_type_code` (出口退税类代码) - 从业务系统获取
- `refined_oil_abnormal_flag` (成品油异常标识) - 非成品油固定为"9"
- `tax_classification_code` (商品税收分类编码) - 从业务系统获取
- **11个特定要素字段** - 详见第3.3节，长软API完全不返回，需从其他数据源获取

### 7.4 数电专票特殊集成要点

⚠️ **这是最重要的部分，必须根据业务特性详细说明！**

#### 7.4.1 发票类型路由

**挑战**：长软对所有数电票统一返回类型码09，需要根据QDLX路由到正确的处理器。

**解决方案**：
1. 检查 `HEAD/FPLX` 是否为 "09"
2. 检查 `HEAD/QDLX` 的值
3. 根据QDLX路由：
   - QDLX=20 → 使用本映射表（数电专票）
   - QDLX=10 → 路由到数电普票映射表
   - QDLX=01 → 路由到数电纸质专票映射表
   - 其他值 → 返回不支持的数电类型错误

**业务背景**：
- 数电发票是中国税务总局推出的全电发票，通过电子税务局开具
- 长软系统将所有数电发票归为一个大类（09），通过QDLX细分
- 本公司API需要将数电专票映射为独立类型（81）

#### 7.4.2 四方信息缺失处理

**挑战**：数电票简化了四方信息，长软返回的地址电话、开户行账号均为空。

**解决方案**：
1. **接受空值**：buyer_address、buyer_phone等字段返回null
2. **业务规则补全**（可选）：
   - 如果业务系统已有购销双方完整信息，可从业务系统补全
   - 如果无法补全，返回null，不影响查验结果
3. **前端提示**：在展示数电票时，提示用户"数电票无需提供详细地址电话"

**注意事项**：
- 不要因为四方信息缺失而认为查验失败
- 数电票的简化是税务总局的政策要求，属于正常现象
- 只要购销双方名称和税号一致即可

#### 7.4.3 发票号码唯一性

**挑战**：数电票无发票代码，仅靠20位发票号码识别。

**解决方案**：
1. **存储策略**：
   - invoice_number: 20位发票号码（主键）
   - invoice_code: null（数电票无代码）
   - invoice_type: "81"（标识为数电专票）

2. **查询策略**：
   - 用户查验时只需提供发票号码
   - 不需要提供发票代码和校验码
   - 开票日期和金额作为辅助验证

3. **去重策略**：
   - 以 (invoice_number, invoice_type) 作为唯一键
   - 同一张数电票的多次查验应能识别为同一张票

#### 7.4.4 红字发票链条追踪

**挑战**：数电票红字发票需要追踪原蓝字发票。

**解决方案**：
1. **识别红字发票**：
   - 检查 `ZFBZ` 是否为 3/7/8
   - 设置 `is_blue_invoice = 'N'`

2. **记录原票信息**（需要业务系统支持）：
   - `original_blue_invoice_no`: 原蓝字发票号码
   - 建立红蓝字发票关联关系

3. **红字列表处理**：
   - 长软09类型暂不返回红字列表（HZLIST）
   - 需要从业务系统维护红蓝字关系

**业务背景**：
- 数电票不支持作废，只能通过红字发票冲销
- 一张蓝字发票可能对应多张部分红字发票
- 需要建立完整的红蓝字发票链条追踪机制

#### 7.4.5 明细数据完整性验证

**挑战**：确保明细数据与汇总数据一致。

**解决方案**：
与传统专票相同，参见 [本公司01_增值税专用发票→长软01.md](./本公司01_增值税专用发票→长软01.md) 第7.4.2节。

---

## 版本历史

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|---------|------|
| v1.1 | 2025-12-30 | 添加3.3节"特定要素字段说明"，详细说明11个特定要素字段在长软API中缺失的情况及处理策略 | Claude |
| v1.0 | 2025-12-30 | 初始版本，创建完整的数电专票映射表 | Claude |

---

## 参考文档

1. 本公司API定义: [`/partners/invoice-verifications`](https://s.apifox.cn/2795ad32-31e7-4363-8294-578e8dc6202c/395314528e0) (Definition ID: 231697052)
2. 长软API文档: `【长软】发票查验服务调用说明V4.0.16.pdf` (第13-14页)
3. 通用枚举映射: `00_通用说明_枚举映射.md`
4. 通用请求参数映射: `00_通用说明_请求参数映射.md`
5. 国家税务总局全电发票平台: https://etax.chinatax.gov.cn/

---

**文档结束**
